# The validation scripts need numpy only. scipy and pandas are not imported
# anywhere yet and are deliberately not pinned, so nothing fails to install
# on a Python version they do not have wheels for.
numpy>=1.26
