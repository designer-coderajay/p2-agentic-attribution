[pytest]
testpaths = tests
# `src` on the path so tests import the library exactly as scripts/ does. No
# packaging step, no editable install, nothing between a clean checkout and a
# green run.
pythonpath = src
addopts = -q --strict-markers --strict-config
filterwarnings =
    error
